# =============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# =============================================================================
# Bu faylni serverga copy-paste qilishdan oldin barcha qiymatlarni to'ldiring!

# -----------------------------------------------------------------------------
# Telegram Bot Configuration
# -----------------------------------------------------------------------------
# BotFather dan olingan bot tokenini kiriting
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
BOT_USERNAME=your_bot_username

# -----------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# -----------------------------------------------------------------------------
# Docker Compose ishlatganda host: postgres (service name)
# Tashqaridan ulanish uchun host: localhost yoki server IP
POSTGRES_USER=azizbot
POSTGRES_PASSWORD=SecurePass2026!@#
POSTGRES_DB=aziz_bot_db
DB_PORT=5432

# Prisma uchun DATABASE_URL (Docker ichida)
# DIQQAT: Host nomi 'postgres' bo'lishi kerak (docker-compose service name)
DATABASE_URL=postgresql://azizbot:SecurePass2026!@#@postgres:5432/aziz_bot_db?schema=public&connect_timeout=30&pool_timeout=30&socket_timeout=30

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
NODE_ENV=production
PORT=3000

# Log Level: error, warn, info, debug
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Web Panel Configuration
# -----------------------------------------------------------------------------
# Server IP manzilingizni qo'ying (masalan: http://161.35.172.239:3000/admin/)
WEB_PANEL_URL=http://YOUR_SERVER_IP:3000/admin/

# -----------------------------------------------------------------------------
# Payment Configuration (PayMe)
# -----------------------------------------------------------------------------
# PayMe test mode (ishlatilmayotgan bo'lsa bo'sh qoldiring)
PAYME_MERCHANT_ID=
PAYME_KEY=
PAYME_TEST_KEY=
PAYME_ENDPOINT=https://checkout.paycom.uz

# -----------------------------------------------------------------------------
# Default Channel Configuration (Optional)
# -----------------------------------------------------------------------------
# Agar default kanal qo'shmoqchi bo'lsangiz:
# DEFAULT_DATABASE_CHANNEL_LINK=@your_channel
# DEFAULT_DATABASE_CHANNEL_NAME=My Channel
